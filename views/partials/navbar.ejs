<nav class="navbar navbar-expand-lg fixed-top modern-navbar">
  <div class="container-fluid px-3 px-lg-5">
    <!-- Brand with animated icon -->
    <a class="navbar-brand" href="/">
      <div class="brand-wrapper">
        <i class="fa-solid fa-microchip brand-icon"></i>
        <span class="brand-text">Study<span class="text-gradient">Craft</span></span>
      </div>
    </a>

    <!-- Mobile Toggle with animation -->
    <button class="navbar-toggler custom-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar" 
            aria-controls="mainNavbar" aria-expanded="false" aria-label="Toggle navigation">
      <div class="animated-icon">
        <span></span>
        <span></span>
        <span></span>
      </div>
    </button>

    <!-- Navbar Links -->
    <div class="collapse navbar-collapse" id="mainNavbar">
      <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link <%= page === 'home' ? 'active' : '' %>" href="/">
            <i class="fa-solid fa-home nav-icon"></i>
            <span class="nav-label">Home</span>
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= page === 'dashboard' ? 'active' : '' %>" href="/dashboard">
            <i class="fa-solid fa-chart-pie nav-icon"></i>
            <span class="nav-label">Dashboard</span>
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= page === 'scheduler' ? 'active' : '' %>" href="/scheduler">
            <i class="fa-solid fa-calendar-check nav-icon"></i>
            <span class="nav-label">Scheduler</span>
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= page === 'tasks' ? 'active' : '' %>" href="/tasks">
            <i class="fa-solid fa-list-check nav-icon"></i>
            <span class="nav-label">Tasks</span>
          </a>
        </li>
        
        <!-- Tools Dropdown -->
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="toolsDropdown" role="button" 
             data-bs-toggle="dropdown" aria-expanded="false">
            <i class="fa-solid fa-calculator nav-icon"></i>
            <span class="nav-label">Tools</span>
          </a>
          <ul class="dropdown-menu dropdown-menu-end modern-dropdown" aria-labelledby="toolsDropdown">
            <li>
              <a class="dropdown-item <%= page === 'calculator' ? 'active' : '' %>" href="/calculator">
                <i class="fa-solid fa-flask me-2"></i>Scientific Calculator
              </a>
            </li>
            <li>
              <a class="dropdown-item <%= page === 'matrix' ? 'active' : '' %>" href="/matrix">
                <i class="fa-solid fa-table-cells me-2"></i>Matrix Calculator
              </a>
            </li>
            <li>
              <a class="dropdown-item <%= page === 'crypto-calculator' ? 'active' : '' %>" href="/crypto-calculator">
                <i class="fa-solid fa-lock me-2"></i>Crypto Calculator
              </a>
            </li>
            <li><hr class="dropdown-divider"></li>
            <li>
              <a class="dropdown-item" href="/calculator">
                <i class="fa-solid fa-grid-2 me-2"></i>All Tools
              </a>
            </li>
          </ul>
        </li>
      </ul>

      <!-- Right Side - User Menu -->
      <div class="d-flex align-items-center gap-2">
        <% if (currentUser) { %>
          <!-- Notifications -->
          <!-- <div class="dropdown">
            <button class="btn btn-icon notification-btn" type="button" data-bs-toggle="dropdown" 
                    aria-expanded="false" id="notificationBell">
              <i class="fa-regular fa-bell"></i>
              <span class="notification-badge" id="navbarNotificationCount">3</span>
            </button>
            <div class="dropdown-menu dropdown-menu-end notification-dropdown" aria-labelledby="notificationBell">
              <div class="notification-header d-flex justify-content-between align-items-center">
                <span class="notification-title">Notifications</span>
                <button class="btn btn-sm btn-link" id="navbarMarkAllRead" type="button">Mark all read</button>
              </div>
              <div class="notification-list-wrapper">
                <ul class="notification-list" id="navbarNotificationList">
                  <li class="notification-empty text-center py-3 text-muted">
                    <i class="fa-regular fa-bell-slash me-2"></i>No notifications
                  </li>
                </ul>
              </div>
              <div class="notification-footer text-center">
                <a href="/dashboard" class="btn btn-link btn-sm">View All</a>
              </div>
            </div>
          </div> -->

          <!-- User Dropdown -->
          <div class="dropdown">
            <button class="btn user-dropdown-btn" type="button" data-bs-toggle="dropdown" 
                    aria-expanded="false">
              <div class="user-avatar-wrapper">
                <% if (currentUser.avatarUrl) { %>
                  <img src="<%= currentUser.avatarUrl %>" alt="<%= currentUser.fullName %>" class="user-avatar">
                <% } else { %>
                  <div class="user-avatar-placeholder">
                    <%= currentUser.fullName ? currentUser.fullName.charAt(0).toUpperCase() : 'U' %>
                  </div>
                <% } %>
                <span class="user-status"></span>
              </div>
              <span class="user-name d-none d-lg-inline"><%= currentUser.fullName ? currentUser.fullName.split(' ')[0] : 'User' %></span>
              <i class="fa-solid fa-chevron-down ms-1"></i>
            </button>
            <ul class="dropdown-menu dropdown-menu-end modern-dropdown user-dropdown">
              <li>
                <div class="dropdown-user-info">
                  <div class="user-fullname"><%= currentUser.fullName || 'User' %></div>
                  <div class="user-email"><%= currentUser.email || '' %></div>
                </div>
              </li>
              <li><hr class="dropdown-divider"></li>
              <li>
                <a class="dropdown-item" href="/profile">
                  <i class="fa-regular fa-user me-2"></i>Profile
                </a>
              </li>
              <li><hr class="dropdown-divider"></li>
              <li>
                <button class="dropdown-item text-danger" id="logoutBtn">
                  <i class="fa-regular fa-sign-out me-2"></i>Logout
                </button>
              </li>
            </ul>
          </div>
        <% } else { %>
          <!-- Auth Buttons for Non-logged in users -->
          <div class="auth-buttons">
            <a href="/login" class="btn btn-outline-primary btn-sm login-btn">
              Login
            </a>
            <a href="/signup" class="btn btn-primary btn-sm signup-btn">
              </i>Sign Up
            </a>
          </div>
        <% } %>

        <!-- Theme Toggle -->
        <!-- <button class="btn btn-icon theme-toggle" id="themeToggle" aria-label="Toggle theme">
          <i class="fa-regular fa-moon"></i>
        </button> -->
      </div>
    </div>
  </div>
</nav>

<style>
  /* Modern Navbar Styles */
  .modern-navbar {
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
    padding: 0.8rem 0;
    transition: all 0.3s ease;
    border-bottom: 1px solid rgba(255, 255, 255, 0.3);
  }

  /* Dark mode support */
  body.dark-mode .modern-navbar {
    background: rgba(18, 18, 18, 0.95);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  /* Brand Styles */
  .brand-wrapper {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .brand-icon {
    font-size: 2rem;
    color: #4361ee;
    animation: pulse 2s infinite;
  }

  .brand-text {
    font-size: 1.5rem;
    font-weight: 700;
    background: linear-gradient(135deg, #2b2d42 0%, #4361ee 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .text-gradient {
    background: linear-gradient(135deg, #4361ee, #7209b7);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  body.dark-mode .brand-text {
    background: linear-gradient(135deg, #fff 0%, #4361ee 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  @keyframes pulse {
    0% { transform: scale(1); }
    50% { transform: scale(1.05); }
    100% { transform: scale(1); }
  }

  /* Navigation Links */
  .navbar-nav .nav-link {
    padding: 0.8rem 1.2rem;
    margin: 0 0.2rem;
    font-weight: 500;
    color: #4a4a4a;
    border-radius: 12px;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    position: relative;
  }

  body.dark-mode .navbar-nav .nav-link {
    color: #e0e0e0;
  }

  .navbar-nav .nav-link:hover {
    background: rgba(67, 97, 238, 0.1);
    color: #4361ee;
    transform: translateY(-2px);
  }

  .navbar-nav .nav-link.active {
    background: linear-gradient(135deg, #4361ee, #7209b7);
    color: white !important;
    box-shadow: 0 4px 15px rgba(67, 97, 238, 0.3);
  }

  .navbar-nav .nav-link.active i {
    color: white;
  }

  .nav-icon {
    font-size: 1.1rem;
    color: #666;
    transition: all 0.3s ease;
  }

  .navbar-nav .nav-link:hover .nav-icon {
    color: #4361ee;
    transform: scale(1.1);
  }

  /* Custom Toggler Animation */
  .custom-toggler {
    border: none;
    padding: 0.5rem;
    background: transparent;
  }

  .animated-icon {
    width: 30px;
    height: 20px;
    position: relative;
    margin: 0;
    transform: rotate(0deg);
    transition: 0.5s ease-in-out;
    cursor: pointer;
  }

  .animated-icon span {
    display: block;
    position: absolute;
    height: 3px;
    width: 100%;
    background: #4361ee;
    border-radius: 9px;
    opacity: 1;
    left: 0;
    transform: rotate(0deg);
    transition: 0.25s ease-in-out;
  }

  .animated-icon span:nth-child(1) { top: 0px; }
  .animated-icon span:nth-child(2) { top: 8px; }
  .animated-icon span:nth-child(3) { top: 16px; }

  .custom-toggler[aria-expanded="true"] .animated-icon span:nth-child(1) {
    top: 8px;
    transform: rotate(135deg);
  }

  .custom-toggler[aria-expanded="true"] .animated-icon span:nth-child(2) {
    opacity: 0;
    left: -60px;
  }

  .custom-toggler[aria-expanded="true"] .animated-icon span:nth-child(3) {
    top: 8px;
    transform: rotate(-135deg);
  }

  /* Dropdown Styles */
  .modern-dropdown {
    border: none;
    border-radius: 16px;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
    padding: 0.8rem;
    margin-top: 0.8rem;
    background: rgba(255, 255, 255, 0.98);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    animation: dropdownFade 0.3s ease;
  }

  body.dark-mode .modern-dropdown {
    background: rgba(30, 30, 30, 0.98);
  }

  @keyframes dropdownFade {
    from {
      opacity: 0;
      transform: translateY(-10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .dropdown-item {
    padding: 0.8rem 1.2rem;
    border-radius: 12px;
    transition: all 0.3s ease;
    font-weight: 500;
  }

  .dropdown-item:hover {
    background: rgba(67, 97, 238, 0.1);
    transform: translateX(5px);
  }

  .dropdown-item.active {
    background: linear-gradient(135deg, #4361ee, #7209b7);
    color: white;
  }

  /* User Dropdown Styles */
  .user-dropdown-btn {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.4rem 0.8rem;
    border-radius: 40px;
    background: rgba(67, 97, 238, 0.1);
    border: 2px solid transparent;
    transition: all 0.3s ease;
  }

  .user-dropdown-btn:hover {
    background: rgba(67, 97, 238, 0.15);
    border-color: #4361ee;
  }

  .user-avatar-wrapper {
    position: relative;
    width: 32px;
    height: 32px;
  }

  .user-avatar, .user-avatar-placeholder {
    width: 100%;
    height: 100%;
    border-radius: 50%;
    object-fit: cover;
  }

  .user-avatar-placeholder {
    background: linear-gradient(135deg, #4361ee, #7209b7);
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 600;
    font-size: 1rem;
  }

  .user-status {
    position: absolute;
    bottom: 2px;
    right: 2px;
    width: 8px;
    height: 8px;
    background: #10b981;
    border-radius: 50%;
    border: 2px solid white;
  }

  body.dark-mode .user-status {
    border-color: #1e1e1e;
  }

  .user-name {
    font-weight: 600;
    color: #333;
  }

  body.dark-mode .user-name {
    color: #e0e0e0;
  }

  .dropdown-user-info {
    padding: 1rem;
    text-align: center;
  }

  .user-fullname {
    font-weight: 700;
    color: #333;
    margin-bottom: 0.2rem;
  }

  .user-email {
    font-size: 0.85rem;
    color: #666;
  }

  body.dark-mode .user-fullname {
    color: #e0e0e0;
  }

  body.dark-mode .user-email {
    color: #999;
  }

  /* Notification Styles */
  .notification-btn {
    position: relative;
    padding: 0.5rem;
    border-radius: 50%;
    background: transparent;
    border: 2px solid transparent;
    transition: all 0.3s ease;
  }

  .notification-btn:hover {
    background: rgba(67, 97, 238, 0.1);
    border-color: #4361ee;
  }

  .notification-btn i {
    font-size: 1.3rem;
    color: #666;
  }

  body.dark-mode .notification-btn i {
    color: #e0e0e0;
  }

  .notification-badge {
    position: absolute;
    top: 0;
    right: 0;
    background: #ef4444;
    color: white;
    font-size: 0.7rem;
    font-weight: 600;
    min-width: 18px;
    height: 18px;
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 2px solid white;
  }

  body.dark-mode .notification-badge {
    border-color: #1e1e1e;
  }

  .notification-dropdown {
    width: 320px;
    max-width: 90vw;
    padding: 0;
    border-radius: 14px;
    overflow: hidden;
  }

  .notification-header {
    padding: 0.7rem 0.9rem;
    border-bottom: 1px solid rgba(0, 0, 0, 0.08);
    background: rgba(67, 97, 238, 0.05);
  }

  .notification-title {
    font-weight: 600;
    color: #2b2d42;
  }

  .notification-list-wrapper {
    max-height: 300px;
    overflow-y: auto;
  }

  .notification-list {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .notification-item {
    padding: 0.7rem 0.9rem;
    border-bottom: 1px solid rgba(0, 0, 0, 0.06);
    cursor: pointer;
    transition: background 0.2s ease;
  }

  .notification-item:hover {
    background: rgba(67, 97, 238, 0.06);
  }

  .notification-item.unread {
    background: rgba(67, 97, 238, 0.1);
  }

  .notification-item-title {
    font-weight: 600;
    color: #2b2d42;
    font-size: 0.9rem;
    margin-bottom: 0.15rem;
  }

  .notification-item-message {
    color: #666;
    font-size: 0.82rem;
    line-height: 1.35;
  }

  .notification-item-time {
    color: #888;
    font-size: 0.75rem;
    margin-top: 0.2rem;
  }

  .notification-footer {
    padding: 0.55rem;
    border-top: 1px solid rgba(0, 0, 0, 0.08);
    background: #fff;
  }

  body.dark-mode .notification-header {
    border-bottom-color: rgba(255, 255, 255, 0.08);
    background: rgba(255, 255, 255, 0.03);
  }

  body.dark-mode .notification-title {
    color: #f1f1f1;
  }

  body.dark-mode .notification-item {
    border-bottom-color: rgba(255, 255, 255, 0.06);
  }

  body.dark-mode .notification-item:hover {
    background: rgba(255, 255, 255, 0.04);
  }

  body.dark-mode .notification-item.unread {
    background: rgba(67, 97, 238, 0.2);
  }

  body.dark-mode .notification-item-title {
    color: #f1f1f1;
  }

  body.dark-mode .notification-item-message {
    color: #c7c7c7;
  }

  body.dark-mode .notification-item-time {
    color: #a0a0a0;
  }

  body.dark-mode .notification-footer {
    border-top-color: rgba(255, 255, 255, 0.08);
    background: rgba(30, 30, 47, 0.95);
  }

  /* Auth Buttons */
  .auth-buttons {
    display: flex;
    gap: 0.5rem;
  }

  .login-btn, .signup-btn {
    padding: 0.6rem 1.2rem;
    border-radius: 40px;
    font-weight: 600;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .login-btn {
    border: 2px solid #4361ee;
    color: #4361ee;
  }

  .login-btn:hover {
    background: #4361ee;
    color: white;
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(67, 97, 238, 0.3);
  }

  .signup-btn {
    background: linear-gradient(135deg, #4361ee, #7209b7);
    border: none;
    color: white;
  }

  .signup-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 5px 20px rgba(67, 97, 238, 0.4);
  }

  .signup-btn::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    transition: left 0.5s ease;
  }

  .signup-btn:hover::before {
    left: 100%;
  }

  /* Theme Toggle */
  .theme-toggle {
    padding: 0.5rem;
    border-radius: 50%;
    background: transparent;
    border: 2px solid transparent;
    transition: all 0.3s ease;
  }

  .theme-toggle:hover {
    background: rgba(67, 97, 238, 0.1);
    border-color: #4361ee;
  }

  .theme-toggle i {
    font-size: 1.2rem;
    color: #666;
    transition: all 0.3s ease;
  }

  .theme-toggle:hover i {
    color: #4361ee;
    transform: rotate(180deg);
  }

  /* Mobile Responsive */
  @media (max-width: 991.98px) {
    .navbar-nav .nav-link {
      padding: 0.8rem;
      margin: 0.2rem 0;
    }

    .navbar-nav .nav-link i {
      width: 24px;
    }

    .auth-buttons {
      margin: 1rem 0;
      justify-content: center;
    }

    .user-dropdown-btn {
      width: 100%;
      justify-content: center;
    }

    .theme-toggle {
      position: absolute;
      top: 1rem;
      right: 4rem;
    }
  }

  @media (max-width: 576px) {
    .brand-text {
      font-size: 1.2rem;
    }

    .brand-icon {
      font-size: 1.5rem;
    }

    .auth-buttons {
      flex-direction: column;
    }

    .login-btn, .signup-btn {
      width: 100%;
      text-align: center;
    }
  }

  /* Loading Animation */
  @keyframes shimmer {
    0% { background-position: -1000px 0; }
    100% { background-position: 1000px 0; }
  }

  .navbar.loading {
    animation: shimmer 2s infinite;
    background: linear-gradient(to right, #f6f7f8 0%, #edeef1 20%, #f6f7f8 40%, #f6f7f8 100%);
    background-size: 1000px 100%;
  }
</style>